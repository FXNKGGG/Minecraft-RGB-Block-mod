# Project properties
org.gradle.jvmargs=-Xmx2G -Xms1G

# Define the Java version to use
javaVersion=21

# Enable parallel execution of tasks for faster builds
org.gradle.parallel=true

# Enable Gradle's build cache (this can speed up builds)
org.gradle.caching=true

# Suppress deprecation warnings (optional)
org.gradle.warning.mode=all

# Set the Minecraft version
minecraft.version=1.20.6

# Define your mod's version (optional)
modVersion=1.0.0

# Set Fabric API version (ensure this matches the version you're using)
fabricApiVersion=0.74.0+1.20.6
